#!/usr/bin/env fish
networksetup -setsecurewebproxy Wi-Fi 127.0.0.1 1080
sslocal \
    -b 127.0.0.1:1080 \
    --protocol http \
    --daemonize \
    --dns google \
    --acl $DOTFILES_ROOT/config/shadowsocks.acl \
    --server-url $SHADOWSOCKS_SERVER_URL
