#!/usr/bin/env bash
set -euo pipefail
set -x

brew update
brew bundle --file $DOTFILES_ROOT/Brewfile
mise upgrade
brew bundle cleanup --file $DOTFILES_ROOT/Brewfile --zap --force &
mise prune --yes &
