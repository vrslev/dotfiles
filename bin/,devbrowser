#!/usr/bin/env python3.10
import subprocess
from contextlib import suppress

subprocess.check_call(["defaultbrowser", "chrome"])

with suppress(KeyboardInterrupt):
    input()

subprocess.check_call(["defaultbrowser", "safari"])
