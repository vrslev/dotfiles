{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Python — auto-type-annotate",
            "type": "shell",
            "command": "cd ${workspaceFolder} && auto-type-annotate ${file}",
            "presentation": {
                "reveal": "never",
                "close": true
            }
        }
    ],
}
